# Deployed v1.8.250 schema regeneration (docs f7925f2)

Status: coded, not merged. PR #279 open on `agent/v1-8-250-regen`, branched from `agent/nullable-ref-normaliser-and-aliases` at `0fcff5c` (PR #277, unchanged and included) on `origin/main` `86d7174`; regeneration head `fcb524a86c8bb97f5793c53bb4230c3bec8b6df4`, review fix `04218f2`, Go union member shims `a1bbfdb1900fe0b86b87d74c1ba55cf9ca445e64` (the code head), plus the evidence commits. Supersedes #277 (its commits ride here) and #276 (the transitional v1.8.249 regeneration, not reused). No merge, publish, tag or bot-branch push.

## Inputs

- SDK base: `origin/agent/nullable-ref-normaliser-and-aliases` `0fcff5c` (#277: normaliser rule for `anyOf: [{ $ref }, { type: "null" }]` and the data-driven deprecated-alias tables), itself on `origin/main` `86d7174`.
- OpenAPI source: `Sendmux/docs` main `f7925f2` (`docs(api): publish the v1.8.250 OpenAPI snapshot`), read from a local checkout through `OPENAPI_INPUT_DIR` on every generator run.
- `openapi-app.json` SHA-256 `b42ce87003ae8d4ac80024ce9796cd96db97c5439c5e99d957682f6b6a1f5999` (raw bytes; the file is stored in the canary's `stableStringify` form, so raw and canonical hashes coincide). The live `https://app.sendmux.ai/api/v1/openapi.json`, fetched before the RED run, has the same canonical form (`02-canonical-hash-docs-vs-live.log`; the publication guard below is the same comparison). The figure quoted for this document elsewhere, `5721133e…`, is `json.dumps(document, sort_keys=True, separators=(",", ":"))` of the same bytes — reproduced for docs and live. `openapi-sending.json` unchanged at `c1f82f9b…`. The vendored MCP snapshot before this change was `b901839a…` (the v1.8.248 shape `main`'s packages were generated from).
- Schema delta versus that vendored snapshot (191 components both, no path or operation change): 15 nullable-reference sites are `anyOf: [{ $ref }, { type: "null" }]` against the canonical component (`from` on `MailboxMessage`, `MailboxMessageSummary`, `MailboxRealtimeMessage`, `MailboxMessageContent.participants`; `last_message` on `MailboxThread`/`MailboxThreadSummary`; `MailboxBatchGetItem.content`/`raw_body`; `MailboxSubmissionEnvelope.mail_from`; `MailboxCreateResult.credential`; `ProviderQuotas.per_*`; `SharedAmazonSesLimitRequestPage.pending_request`), no duplicate null branches, no `…OrNull` component; `MailboxMessage` gains the required `message_id`, `in_reply_to`, `references` (`string[]`) and `reply_to` (`MailboxAddress[]`); `MailboxCredential`, `MailboxMessageContent`, `MailboxRawBody`, `MailboxSubmissionEnvelopeAddress` and `ProviderQuotaRange` are plain objects at definition; `MailboxRealtimeMessage`, `MailboxSendScope` and `MailboxSubmissionEnvelope` stay nullable at definition (`02-snapshot-structure.log`). Wire format unchanged: the four fields were always returned and every nullable field stays nullable.
- Toolchain: node v24.21.0, pnpm 10.22.0, Go 1.23.4 (`GOTOOLCHAIN=go1.27.1` for golangci-lint and apidiff, as CI `verify`), PHP 8.4.10 / Composer 2.8.9, Python 3.12.8, Ruby 3.4.1, Rust 1.82.0 floor + stable, Java 17 for openapi-generator.

## RED

`OPENAPI_INPUT_DIR=<docs> pnpm drift:check` on `0fcff5c`: `normalize:check` and every generator ran clean — the #277 alias planner reported no pending or missing replacement — and `verify:sdk-staleness` failed with "Generated SDK package directories are stale." listing 62 entries (50 modified, 12 deleted, 0 untracked): `go/{mailbox,management}` 4 + 4, PHP mailbox 9 / management 5, Python mailbox 11 / management 8, MCP 2, Ruby mailbox 10 / management 7, TS mailbox 1 / management 1. Sending, CLI, Rust and `go/sending` did not change; nothing outside the generated directories changed. Log `.claude/artifacts/l17f/01-drift-check-red.log`; stale list `01-red-stale-list.txt`; tree state `02-git-status-after-regen.txt`, `02-git-diff-stat.txt`.

MCP RED (published 2.1.2 contract): 13 `allOf` branches typed `["object","null"]` beside a closed object (a JSON `null` cannot satisfy them) and `mailbox_get_message` result.data with 18 properties, without the four fields (`04-mcp-analysis-vs-2.1.2.log`).

## Surface delta versus the published packages

Baselines are the published tags (`ts-mailbox-v2.0.1`, `ts-management-v2.0.1`, `go/v2.0.0`, `python-mailbox-v2.0.1`, `python-management-v2.0.0`, `ruby-mailbox/v2.0.1`, `ruby-management/v2.0.0`); PHP against `origin/main` `86d7174`, the split repositories' `v2.1.0`/`v2.1.1` already differing from it in unrelated files. Extraction = L17c's per-language type/field extractor (`05-surface-baseline-published.json`, `05-surface-tree.json`, delta `05-surface-delta-vs-published.txt`). Every number equals L17c's B3 prediction.

| Package (published) | Types removed / added · fields retyped / added | Detail | Verdict | Commit |
| --- | --- | --- | --- | --- |
| `@sendmux/mailbox` 2.0.1 | 0 / 0 · 3 / 8 | `MailboxMessage` + the four fields; `MailboxBatchGetItem.content`/`raw_body` and `MailboxSubmissionEnvelope.mail_from` carry `\| null` themselves while `MailboxMessageContent`, `MailboxRawBody`, `MailboxSubmissionEnvelopeAddress` drop it from their own definitions (compile-time only for code annotating a nullable field with those names); `& { [key: string]: unknown }` wrappers on the content/raw-body/thread-content `data` and `rcpt_to` gone; `from: MailboxAddress \| null`, `send_scope` unchanged | additive; no export removed | `fix(ts-mailbox)` `3c7ca7e` |
| `@sendmux/management` 2.0.1 | 0 / 0 · 5 / 1 | `MailboxCreateResult.credential`, `ProviderQuotas.per_*` carry `\| null`; `MailboxCredential`, `ProviderQuotaRange` plain; `MailboxAppPasswordResult.credential: MailboxCredential`; `pending_request: SharedAmazonSesLimitRequest \| null` unchanged | additive; no export removed | `fix(ts-management)` `2f4a156` |
| `sendmux.ai/go/v2` 2.0.0 | mailbox 25 / 0 · 4 / 4; management 18 / 0 · 1 / 8 (+8 union members renamed) | all 43 removed identifiers are declared by `deprecated_aliases.go` (`type Old = New`, 7 + 9 `NewNil…` constructors, 7 + 0 enum constants; `gofmt -l` clean); the former member API of the eight quota unions (discriminator constant, `Is`/`Set`/`Get`, constructor — 8 constants, 24 methods, 8 constructors) is kept as shims over the `ProviderQuotaRange` and Null members (section below); `MailboxMessage` + `MessageID`/`InReplyTo`/`References`/`ReplyTo`; residual below | aliases and shims keep every old identifier and method compiling and encoding the same bytes | `fix(go)` `99bf0b1` + `a1bbfdb` |
| `sendmux/mailbox` (Packagist 2.1.0; 3.0 unreleased) | 4 / 0 · 4 / 4 | `getData()` ×3 and `getRcptTo()` typed by the canonical models; four `class_alias` shims (`E_USER_DEPRECATED`, until 4.0); four `MailboxMessage` getters added | additive; old names load | `fix(php-mailbox)` `8161af0` |
| `sendmux/management` (Packagist 2.1.1) | 2 / 0 · 1 / 0 | `getCredential(): MailboxCredential`; two `class_alias` shims (until 3.0) | additive; old names load | `fix(php-management)` `09a6983` |
| `sendmux-mailbox` (PyPI 2.0.1) | 4 / 0 · 4 / 4 | `data` ×3 / `rcpt_to` typed by the canonical models; four names resolve through `__getattr__` (one `DeprecationWarning`, `is` the replacement, absent from `__all__`); `var_from: Optional[MailboxAddress]`, `raw_body` unchanged | additive; old names import | `fix(python-mailbox)` `88c40fa` |
| `sendmux-management` (PyPI 2.0.0) | 2 / 0 · 2 / 0 | `credential: MailboxCredential`; per-day union member `ProviderQuotaRange`; two alias names; `pending_request` unchanged | additive; old names import | `fix(python-management)` `d726a98` |
| `sendmux-mcp` (PyPI 2.1.2) | 54 tools, 0 added/removed/renamed, 0 `input_schema` changes, 10 `output_schema` changes | snapshot `b901839…` → `b42ce870…`; unsatisfiable compositions 13 → 0, duplicate null branches 0; `mailbox_get_message` result.data 22 props / 21 required with the four fields, `from` `anyOf: [object, null]` | fix | `fix(python-mcp)` `2b9b2a5` |
| `sendmux-mailbox` (RubyGems 2.0.1) | 4 / 0 · 4 / 4 | same shape under `Sendmux::Mailbox::Generated`; four `deprecate_constant` aliases; `from`, `send_scope` unchanged | additive; old constants resolve | `fix(ruby-mailbox)` `ea211b7` |
| `sendmux-management` (RubyGems 2.0.0) | 2 / 0 · 1 / 0 | `credential` typed `MailboxCredential`; `ProviderCreateBodyQuotasPerDay` accepts `ProviderQuotaRange` or `Integer`; two aliases; `pending_request` unchanged | additive; old constants resolve | `fix(ruby-management)` `ab2af52` |
| `sendmux` crate (crates.io 0.5.0) | — | provenance pins `rust/operation-decisions.json:3`, `rust/src/generated/mod.rs:26` → `b42ce870…`; no Rust type models a changed component | none | `chore(rust)` `fcb524a` |

`grep -rl OrNull` over every generated tree: none. Every field typed by a canonical component keeps its type in every language; the only retypes replace a synthesised artefact class with the canonical class the artefact name now aliases.

### Go release type: breaking (decided by ROOT 2026-09-21)

`apidiff` against `go/v2.0.0` after the shims still lists, and no alias or shim can cover, two classes of change: the eight exported union member fields `Provider{Create,Update}BodyQuotasPer{Day,Hour,Minute,Second}.Nil<Union>1` are removed (the member is now the `ProviderQuotaRange` field), and five exported fields change from a nullable wrapper to the plain canonical type (`MailboxMessageContentResponse.Data`, `MailboxRawBodyResponse.Data`, `MailboxSubmissionEnvelope.RcptTo` elements, `MailboxThreadContentResponse.Data` elements, `MailboxAppPasswordResult.Credential`) because the corrected schema declares them non-nullable, which the app handlers confirm. Go's module tooling selects patch releases automatically, so a compile-breaking 2.0.1 is not acceptable: the Go regeneration is typed `fix(go)!` with a `BREAKING CHANGE` footer, release-please will propose go 3.0.0, and that release is published only together with the `sendmux.ai/go/v3` module path and a "Version 3 migration" README section in a follow-up change-set. The aliases and shims stay so v2 code needs only the import-path change plus the two adjustments above. `go/v2.0.0` keeps working against the live API. The other languages keep the patch types recorded below: their removed names are aliased and their field types follow the wire format.

### Go residual (apidiff, `06-go-apidiff.log` before the shims, `34-go-apidiff-with-shims.log` after, classified in `06b-go-apidiff-classified.txt`)

`apidiff -incompatible` (old = `go/v2.0.0` export data, new = this tree, go1.27.1). mailbox: 44 lines, all on identifiers `deprecated_aliases.go` declares (the type identity moved to the canonical type, so apidiff reports the old name "changed" and its methods "removed"; every reference still compiles — consumer program below). management: 59 lines before the union member shims — 11 of the same nominal kind on `NilMailboxAppPasswordResultCredential` and 48 on the eight `Provider{Create,Update}BodyQuotasPer*` unions — and 27 after them: the 11 nominal lines, 8 × `<Member><Union>: value changed from "<Member>" to "ProviderQuotaRange"` (the former discriminator constant is declared as an alias of the `ProviderQuotaRange` member's constant, so its value follows the union; a `switch s.Type` on the former name compiles and matches the value member), and 8 × `<Union>.<Member>: removed` — the union struct's exported member *field*, read or written directly instead of through `Set`/`Get`. Go has no field aliases and the generated struct is not edited, so that one facet stays removed; the `Set<Member>` and `New<Member><Union>` removals and the `Get`/`Is` method removals are gone. Not listed by apidiff but real: five struct fields drop the `Nil` wrapper of a removed artefact type — `MailboxMessageContentResponse.Data`, `MailboxRawBodyResponse.Data`, `MailboxThreadContentResponse.Data`, `MailboxSubmissionEnvelope.RcptTo`, `MailboxAppPasswordResult.Credential` — so `resp.Data.Value.Body` becomes `resp.Data.Body`. `go/sending`: no change. Both Go commits are typed `fix(go)` (→ 2.0.1) and carry no `BREAKING CHANGE` footer; the direct-field access and the five `Nil`-wrapper retypes are the residual left for the owner to accept in a patch or defer to a `/v3` major.

## Compatibility proofs (`.claude/artifacts/l17f/`)

- Go: a generated program referencing every alias, constructor and constant declared in both `deprecated_aliases.go` files builds and vets (`08-go-consumer-build.log` before the shims: mailbox 25 aliases / 7 constructors / 7 constants, management 18 / 9 / 0; `32-union-shims-green.log` after them: management 18 / 17 / 8; `missing: []`, `extra: []`, "consumer build ok" both times).
- Python: `test_mailbox_deprecated_aliases.py` + `test_management_deprecated_aliases.py` pass on the alias branch, mypy clean on both packages + tests; importing each of the six names yields exactly one `DeprecationWarning` naming the replacement and the removal release, `is` the replacement, and is absent from `__all__` (`09-python-alias-proof.log`).
- Ruby: alias suites 5 runs / 28 assertions + 3 runs / 11 assertions, 0 failures (`10-ruby-alias-proof.log`).
- PHP: `DeprecatedModelAliasesTest` OK (6 tests, 39 assertions) with the shims loaded through PSR-4; phpstan level max `[OK] No errors` (`11-php-alias-proof.log`).
- `MailboxMessage` deserialisation, every language, from two fixtures (`fixtures/`): a message with `from`, `reply_to` and the reply headers populated, and one with `from: null` and the four arrays empty. Both validate against the live document's `MailboxMessage` schema and against the MCP `mailbox_get_message` result.data schema (Draft 2020-12, format checks; the negative controls — `message_id` removed, `from` a string — fail) (`12-fixture-validation.log`). Python `MailboxMessage.from_json` + `to_dict` round trip (`15-proof-python.log`, run with `-W error::DeprecationWarning`); Ruby `build_from_hash` + `valid?` + `to_hash` (`15-proof-ruby.log`); PHP `ObjectSerializer::deserialize` + `valid()` + JSON round trip (`15-proof-php.log`); Go `UnmarshalJSON`/`json.Unmarshal` + `MarshalJSON` round trip, plus alias-typed variables holding canonical values (`15-proof-go.log`); TypeScript: the fixture typed as `MailboxMessage` with the four fields and `from: MailboxAddress | null` compiles under `tsc --strict` against the built `dist/` types, three negative assignments fail (`TS2322` ×2, `TS2339`), and the file runs under node (`15-proof-ts.log`).
- MCP from the built package: wheel built with `python -m build`, installed into a fresh venv, `tools/list` through `fastmcp.Client` on `create_server` per surface (`14-mcp-tools-list.log`, `14-mcp-tools-list-built.json`): 54 tools, 0 added/removed/renamed, 0 `input_schema` changes versus the published 2.1.2 contract, 10 `output_schema` changes, scan `nullAllOf 0 / closedAllOf 0 / dupNull 0`, `mailbox_get_message` result.data 22 props / 21 required with the four fields; the listing equals the committed `mcp-contract.json` tool for tool (54 compared, 0 differing). The wheel embeds the `b42ce870…` snapshot and the committed contract (`e7a0e915…`).

## Commits

One commit per package path, in #276's order; 1–9 with `--no-verify` because `.githooks/pre-commit` runs `pnpm drift:check`, which regenerates every package and can only be green on the complete tree; 10 and 11 ran with the hook and left a clean tree (`17-commits-10-11-hooked.log`; the generated files' mtimes show the regeneration during both commits, and `git status` stayed empty), which also shows the regeneration is deterministic. No `Claude-Session:` trailers.

- `3c7ca7e` fix(ts-mailbox): regenerate mailbox types from the deployed v1.8.250 schema
- `2f4a156` fix(ts-management): regenerate management types from the deployed v1.8.250 schema
- `99bf0b1` fix(go): regenerate mailbox and management types from the deployed v1.8.250 schema
- `8161af0` fix(php-mailbox): regenerate mailbox models from the deployed v1.8.250 schema
- `09a6983` fix(php-management): regenerate management models from the deployed v1.8.250 schema
- `88c40fa` fix(python-mailbox): regenerate mailbox models from the deployed v1.8.250 schema
- `d726a98` fix(python-management): regenerate management models from the deployed v1.8.250 schema
- `2b9b2a5` fix(python-mcp): publish the deployed v1.8.250 app schema and tool result schemas
- `ea211b7` fix(ruby-mailbox): regenerate mailbox models from the deployed v1.8.250 schema
- `ab2af52` fix(ruby-management): regenerate management models from the deployed v1.8.250 schema
- `fcb524a` chore(rust): sync OpenAPI provenance with the deployed v1.8.250 app schema
- `04218f2` chore(codegen): reject null-only components in the nullable-reference normaliser (review fix, below)
- `a1bbfdb` fix(go): keep the quota union member API of v2.0.0 as deprecated shims (below)

No hand-written code, test or README references a removed or narrowed symbol; the non-generated changes are the two Rust provenance digests, the review fix to the normaliser guard and its test, and the union member shim table, planner, writer and equivalence test.

## Checks on the head

- Rust floor (CI `rust-floor` + `rust-latest-package` fmt): `cargo +1.82.0 test --locked --all-targets --all-features` 9 + 3 + 12 + 5 passed, `--doc` 7 passed, `cargo +stable fmt --check` clean (`18-rust-floor.log`).
- Publication guard `GITHUB_TOKEN=<gh auth token> node scripts/publication-guard.mjs candidate --sha fcb524a86c8bb97f5793c53bb4230c3bec8b6df4`: "Live OpenAPI specs match committed snapshots." with app `b42ce870…` and sending `c1f82f9…` (`21-publication-guard-fcb524a86c8bb97f5793c53bb4230c3bec8b6df4.log`).
- CI verify replica on the code head `a1bbfdb` (`37-ci-verify-replica-a1bbfdb1900fe0b86b87d74c1ba55cf9ca445e64.log`): `OPENAPI_INPUT_DIR=<docs> pnpm build && pnpm test:publication-guard && pnpm test:runtime-contracts` exit 0 (`REPLICA_EXIT=0 2026-09-21 10:05:38 AEST`; `verify:sdk-staleness` green, `node --test` suites green, golangci-lint 0 issues with the shims and their test in scope); ROOT re-ran `gofmt -l` (clean), `go vet ./...` (0), `go test ./...` (0) and the compiled test binary `-test.run TestDeprecatedQuotaUnionMemberShims -test.v` (parent + 8 subtests PASS), `node --test scripts/deprecated-model-aliases.test.mjs` (pass) and `generate:go` (byte-identical go/ tree). Final PR head `9264a6d3` (this evidence file only): CI 37/37 checks green, `MERGEABLE/CLEAN` at 02:23Z; publication guard `candidate --sha a1bbfdb1900fe0b86b87d74c1ba55cf9ca445e64`: "Live OpenAPI specs match committed snapshots." with app `b42ce870…` and sending `c1f82f9…` (`21-publication-guard-a1bbfdb…log`); Rust floor on `a1bbfdb` (`38-rust-floor-a1bbfdb.log`, `rust/` unchanged since `fcb524a`): `cargo +1.82.0 test --locked --all-targets --all-features` 9 + 3 + 12 + 5 passed, `--doc` 7 passed, `cargo +stable fmt --check` clean.
- CI verify replica on the regeneration head `fcb524a` (`20-ci-verify-replica-fcb524a86c8bb97f5793c53bb4230c3bec8b6df4.log`): `OPENAPI_INPUT_DIR=<docs> GOTOOLCHAIN=go1.27.1 pnpm build && pnpm test:publication-guard && pnpm test:runtime-contracts` exit 0 across 114 pnpm scripts (08:54–09:05) — `drift:check` "Generated SDK package directories have no uncommitted drift", `verify:layout`, `verify:ts-public-api`, `verify:tree-shaking`, `check:cli`, `check:surface-coverage` (104 operations), `check:live-e2e`; Go `go test ./...` ok for `core`, `livee2e`, `mailbox`, `sending` (`management` and `sdk` have no test files), `go vet` clean, `golangci-lint@v2.13.2` "0 issues"; PHP `generate-php.test` green, phpstan "[OK] No errors", PHPUnit "OK (76 tests, 367 assertions)", split dry-runs verified (the `legacy_range` composer failure printed as JSON comes from `scripts/generate-php.test.mjs:102`, which asserts that range is rejected; the test passed); Python mypy "no issues found" (57 + 1 + 4 source files), pytest 163 native + 10 langchain (173 shared, 0 skipped); MCP mypy clean (30 files), pytest 158 passed, `sendmux_mcp.contract --check` and `check-mcp-artifacts` green, conformance required scenarios passed (the 8 non-scored failures are the `tasks-*` extension scenarios); Ruby `check-ruby` 11 + 1 + 14 + 5 + 5 + 3 runs / 248 assertions, 0 failures, 10 gem builds; `node --test` totals across the build 226 tests / 226 pass / 0 fail / 0 skipped; `test:publication-guard` and `test:runtime-contracts` green. Afterwards the tree held only bundler's `Gemfile.lock` rewrite (below), reverted.
- CI on PR #279: code head `fcb524a` run 35543876472 (queued when this file was written); the final state of the code head and of this evidence head is recorded on the PR and in the L17f report. #277's own CI showed the reference roster — 37 passing checks plus `verify` red for the docs-snapshot root cause this PR resolves; `ownership-diagnostic` is a Windows-only skipped job.
- Review: independent reviewer over the 11 commit messages, the per-commit path scope, the `OrNull` grep, the Rust provenance edit and this draft — 0 defects, 1 observation (it had no shell, so it matched commit subjects, stat lines, alias counts, hashes and every table number against the captured logs but could not read commit bodies). Closed mechanically: every backticked identifier in each commit body was searched in that commit's diff — 203 found; the 10 not in any diff are the messages' "unchanged" claims (`getLastMessage()`, `getSendScope()`, `ProviderQuotas::getPer*()`, `getPendingRequest()`, `pending_request`, `ProviderQuotas#per_*`) and the alias file name, each verified present at HEAD and line-identical to `origin/main` (`19-commit-message-identifier-check.log`). CodeRabbit and the settle sweep: recorded on the PR and in the L17f report.
- Root `Gemfile.lock`: bundler rewrote `sendmux-mailbox (2.0.0)` → `(2.0.1)` during the Ruby checks (the gem version on `main` is already 2.0.1) — a pre-existing lag, reverted locally and parked.

## Review fix

CodeRabbit (review `5262094188` on `f2c84e5`, one actionable finding, `scripts/normalize-openapi-for-codegen.mjs:208`): `referencedComponentType` accepted any string `type`, so a nullable reference to a null-only OAS 3.1 component would have been written into the 3.0.3 document as `type: "null"` and failed later in `redocly lint` rather than at the guard. Fixed by validating against the OAS 3.0 type set; new test `a nullable reference to a null-only component fails naming the reference`, seen red first (`24-coderabbit-fix-red.log`: `AssertionError [ERR_ASSERTION]: a null-only target has no OAS 3.0 type and must not normalise silently`, pass 3 / fail 1) then green (`24-coderabbit-fix-green.log`: pass 4 / fail 0). No sibling: the other `type` checks in the normaliser and verifier act on branches already filtered by `isNullSchema`. The pre-commit hook regenerated every package from the real snapshot through the tightened guard and left the tree clean (`25-review-fix-commit-hook.log`).

## Go union member shims (`a1bbfdb`)

The regeneration renamed the object member of the eight `Provider{Create,Update}BodyQuotasPer{Day,Hour,Minute,Second}` unions from `Nil<Union>1` to `ProviderQuotaRange`; type aliases cover the member *type* but not the member *API* a go/v2.0.0 consumer wrote against — per union `U` with former member `M`: the constant `<M><U> <U>Type`, `func (s U) Is<M>() bool`, `func (s *U) Set<M>(v M)`, `func (s U) Get<M>() (M, bool)` and `func New<M><U>(v M) U` (`go/v2.0.0:go/management/oas_schemas_gen.go:10692-10773`).

- RED: a consumer program using all five facets on all eight unions fails to build against `04218f2` — 40 errors, e.g. `providerCreateBodyQuotasPerDay.SetNilProviderCreateBodyQuotasPerDay1 undefined (type management.ProviderCreateBodyQuotasPerDay has no field or method SetNilProviderCreateBodyQuotasPerDay1)`, `undefined: management.NewNilProviderCreateBodyQuotasPerDay1ProviderCreateBodyQuotasPerDay`, `undefined: management.NilProviderCreateBodyQuotasPerDay1ProviderCreateBodyQuotasPerDay` (`28-union-shims-red-consumer.log`, `union-consumer/`); the equivalence test fails to compile for the same reason (`30-union-shims-red-test.log`).
- Table `deprecatedUnionMembers` on the management surface (`scripts/generate-go.mjs`): `{ union, deprecated: "Nil<Union>1", replacementMember: "ProviderQuotaRange", nullableWrapper: "NilProviderQuotaRange" }` × 8. Rule `planDeprecatedUnionMembers` (`scripts/deprecated-model-aliases.mjs`): the union, its `ProviderQuotaRange` member, its `Null` member and the wrapper must be generated (loud throw naming the missing one), the shim is pending while the former member is still generated — three unit tests, red before the rule existed (`29-union-planner-red.log`: `does not provide an export named 'planDeprecatedUnionMembers'`) then green (`29-union-planner-green.log`, 6 / 0). The writer (`renderDeprecatedUnionMember`) asserts every delegated signature against the generated output — the `ProviderQuotaRange<U>` constant, `Is`/`Set`/`Get`/`New` of the `ProviderQuotaRange` member, `IsNull`/`SetNull`/`NewNull<U>`, and `NilProviderQuotaRange{ Value ProviderQuotaRange; Null bool }` — and requires `M` to be an active type alias of the wrapper, so the shim signatures equal the published ones.
- Shims emitted into `go/management/deprecated_aliases.go` (`31-generate-go-with-shims.log`; 18 types, 17 constructors, 8 constants, 24 methods; `gofmt -l` clean): `const <M><U> = ProviderQuotaRange<U>`; `Is<M>` = `IsProviderQuotaRange() || IsNull()` (the former nullable member covered both); `Set<M>` maps `v.Null` to `SetNull(struct{}{})` and a value to `SetProviderQuotaRange(v.Value)`; `Get<M>` returns `M{Value: v}, true` for the value member, `M{Null: true}, true` for the Null member, zero and false for the int member; `New<M><U>` delegates to `Set<M>`. Each carries a `// Deprecated:` line naming the replacement. Read-back divergence, by design: a union set to null through the current `SetNull` also answers `Is<M>()` true and `Get<M>()` `{Null: true}` (the former member itself carried the null case); `Type` equals the former constant only for the value case.
- GREEN (`32-union-shims-green.log`): `go build ./...`, `go vet ./...`, `go test ./...` 39 tests in 6 packages including `TestDeprecatedQuotaUnionMemberShims` (`go/management/deprecated_union_members_test.go`, package-internal so staticcheck's deprecation check stays quiet): for each union, the former and the current API encode byte-identical JSON for a value (`{"max":500,"min":10}`) and for null, both read back through `Get<M>`/`Is<M>` as the input, the value case keeps `Type == <M><U>`, and the int member encodes `7` and reads back zero/false; the old-API consumer builds and vets; the L17e alias consumer regenerated against the new files builds (management 18 / 17 / 8). Mutation red (`35-union-shims-mutation-red.log`): with the null branch of one `Set<M>` disabled, the test fails — `null: former API encodes {"max":0,"min":0}, current API encodes null, want null` — and passes again after restoring the generated file byte-identically. golangci-lint v2.13.2 `0 issues` (`33-golangci-with-shims.log`). apidiff re-run: management 59 → 27 incompatible lines, the `Set<M>`/`New<M><U>` removals gone (Go residual above).
- The commit ran the pre-commit hook (`36-go-union-shims-commit-hook.log`), which regenerated every package from the real snapshot through the extended writer and left the tree clean.

## Release consequences

- release-please after a merge-commit merge with an empty body (a PR title in the merge body is applied to every package path — #257's `628b161` did that): `ts-mailbox` 2.0.2, `ts-management` 2.0.2, then the `node-workspace` cascade `ts-sdk` 2.0.2, `ts-cli` 1.7.2, `ts-ai-sdk` 0.5.2; `python-mailbox` 2.0.2, `python-management` 2.0.1, `python-mcp` 2.1.3 (the hosted MCP image is rebuilt after that publish); `ruby-mailbox` 2.0.2, `ruby-management` 2.0.1; `go` 2.0.1 (see the Go residual); `rust` unchanged apart from the provenance sync riding in the pending 0.5.1; PHP by hand — `sendmux/management` 2.1.2, `sendmux/mailbox` already heads to 3.0.0 on `main`. Umbrella upper bounds (`packages/python/sdk`, `packages/python/langchain`, `packages/ruby/sdk`) stay valid because nothing reaches 3.0.0.
- Closes #277 and #276 as superseded once merged.

## Artifacts

`.claude/artifacts/l17f/` in the main checkout (machine-local): `00-pnpm-install.log`, `01-drift-check-red.log`, `01-red-stale-list.txt`, `02-*.{log,txt}`, `03-{ts,go,php,python,ruby}-generated.diff`, `03-mcp-contract.diff`, `04-mcp-analysis-vs-2.1.2.log`, `05-surface-*.{json,txt}`, `06-go-apidiff.log`, `06b-go-apidiff-classified.txt`, `07-*-setup.log`, `08-go-consumer-build.log` + `go-consumer/`, `09`–`11` alias proofs, `12-fixture-validation.log` + `fixtures/`, `13-ts-workspace-build.log`, `14-mcp-tools-list*.{log,json}`, `15-proof-{python,ruby,php,go,ts}.log` + `proofs/`, `16-commits-1-9.log`, `17-commits-10-11-hooked.log`, `18-rust-floor.log`, `20-ci-verify-replica-*.log`, `21-publication-guard-*.log`, `pids`.
