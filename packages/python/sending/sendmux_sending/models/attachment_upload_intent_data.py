# coding: utf-8

"""
    Sendmux Sending API

    Send emails programmatically via the Sendmux email infrastructure. Every response carries an `X-Request-Id` header; errors include a `retryable` flag. 429 and 503 responses include `Retry-After`. Mutating endpoints accept an `Idempotency-Key` header for safe retries.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class AttachmentUploadIntentData(BaseModel):
    """
    AttachmentUploadIntentData
    """ # noqa: E501
    expires_at: datetime = Field(description="ISO timestamp when the upload URL expires.")
    headers: Dict[str, StrictStr] = Field(description="Headers that must be sent with the PUT upload request.")
    max_size_bytes: StrictInt = Field(description="Maximum upload size in bytes")
    method: StrictStr = Field(description="HTTP method for upload_url")
    upload_id: Annotated[str, Field(strict=True)] = Field(description="Temporary upload intent ID.")
    upload_url: StrictStr = Field(description="Short-lived URL that accepts a binary PUT with the returned headers.")
    __properties: ClassVar[List[str]] = ["expires_at", "headers", "max_size_bytes", "method", "upload_id", "upload_url"]

    @field_validator('method')
    def method_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['PUT']):
            raise ValueError("must be one of enum values ('PUT')")
        return value

    @field_validator('upload_id')
    def upload_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^upl_[a-z0-9]{24}$", value):
            raise ValueError(r"must validate the regular expression /^upl_[a-z0-9]{24}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AttachmentUploadIntentData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AttachmentUploadIntentData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "expires_at": obj.get("expires_at"),
            "headers": obj.get("headers"),
            "max_size_bytes": obj.get("max_size_bytes"),
            "method": obj.get("method"),
            "upload_id": obj.get("upload_id"),
            "upload_url": obj.get("upload_url")
        })
        return _obj
