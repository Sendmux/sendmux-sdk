# Ruby Management 2 lane: main-bound corrections and held release-branch hunks

Recorded 2026-09-18 15:25 Australia/Melbourne. Branch `agent/ruby-management-2-final` over `origin/main` `4ac9f32dc1a3ede7fae19166072042c885688288`. The qualified worktree sat on PR234's bot head `35bfd3ee`; that release commit belongs to release-please and is not carried into this PR.

## Correctness

- Checker: `scripts/check-ruby.mjs:79-92` reads both bounds of each `add_dependency` range and requires `< (floor major + 1).0`; the changed-package manifest-floor enforcement is unchanged (`:90-92`). The entry guard at `:12` lets `scripts/test-ruby-release-guardrails.mjs` import `checkRubyDependencyFloors` with fixture overrides, and that test runs first inside the canonical check (`:13`).
- Docs: `packages/ruby/management/README.md:32` moves the Gemfile example to `~> 2.0` and the `Migrate from 1.x to 2.0` section from `:35` uses only the public entry point (`lib/sendmux/management/client.rb:68`); the list class change is `lib/sendmux_management_generated/models/provider_item_cursor_list_response.rb:51`. `packages/ruby/sdk/README.md:114-116` and `packages/ruby/sdk/CHANGELOG.md:5,7` extend the existing hand-written `Unreleased 2.0 migration` section (convention from dc01472).
- Held for the release branch, in the worktree's `.claude/ruby-management-2-bot-branch.patch`, `-changelog-lead.md` and `-handoff.md`: the Management CHANGELOG lead (the bot regenerates that file; TS c90faa7 shape), `packages/ruby/sdk/sendmux-sdk.gemspec:23` `>= 2.0.0, < 3.0`, and the two matching `Gemfile.lock` lines. `Gemfile:9-13` sources every gem by `path:`, so that floor resolves only once `version.rb` is 2.0.0, which only the bot commit sets; the publisher (`release-please.yml:913-914` → `check-ruby.mjs:15` `bundle install`) runs on the merge commit, so floor, lock and bump must merge together. Every earlier Ruby release did exactly that: merges eff8855, caa3619, a3efc22 and 38f3f09 each carry a `fix(ruby-*)` lock/gemspec commit beside the bot commit.
- Sequencing: Management 2.0.0 does not depend on Sending 1.5.0. `packages/ruby/management/sendmux-management.gemspec:21-25` declares faraday, marcel and `sendmux-core >= 1.3.0, < 2.0` only, and the SDK already admits Sending `>= 1.4.0, < 2.0`. The serial 233 → 234 → 210 → 223 order is shared-manifest merge policy; only the SDK must be last.

## Tests and evidence

- RED, main-based tree plus the held floor: `rbenv exec bundle lock --local` → `Could not find compatible versions … sendmux-management >= 2.0.0, < 3.0 could not be found in source at packages/ruby/management … version solving has failed.` exit 6.
- RED, guardrail mutant with the ceiling hard-coded to 2.0: `Got unwanted exception. Actual message: "packages/ruby/sdk/sendmux-sdk.gemspec must require sendmux-management < 2.0 for floor >= 2.0.0; found < 3.0"`. The prior-session RED against the original literal parser read `… with an explicit >= floor and < 2.0 upper bound`.
- GREEN: guardrails 3/3; canonical `node scripts/check-ruby.mjs` on the rebased tree: bundle 32 gems, five gems built and named, all Ruby syntax, floors, RuboCop 30 files with no offenses, tests 11/37, 1/6, 14/140 and 5/26 runs/assertions with zero failures, errors or skips.
- Candidate-owned `pnpm canary:openapi`: live specs match the committed snapshots (App SHA256 `09b00ae13c88fe4ecc1bb3fd965bb30efd79447d5ea68b5334816a7452d40b58`, Sending `c1f82f9b8944026571d9e66ae84d4bef90e6c575cc8a57127afcdc6e90aa7b0e`).
- Fresh isolated GEM_HOME with public Core 1.3.0, the gem built from this tree and minitest 5.27.0: `test_management_validation.rb` 5/26; installed consumer 5/36, covering 0/1/2-item lists as `ProviderListItem`, detail `variables` empty and non-empty, and both README migration samples executed from the packed README; 153 loaded files all inside the install. `BUNDLE_FROZEN=true bundle check`, `git diff-files --check` and `git diff-tree --check` clean.
- Tests: +`scripts/test-ruby-release-guardrails.mjs` (real-checker fixtures, seen red twice); −0. The pre-commit `pnpm drift:check` hook passed with no generated drift for both commits. The docs commit's first hook run failed because `../sendmux-docs/openapi-app.json` had just been rewritten by another lane (be59eee, 15:22, SHA256 `a53e4956…`, no `/connection` path) and the hook's default input resolution reads that sibling checkout; the rerun pinned `OPENAPI_INPUT_DIR` to the committed `packages/python/mcp/sendmux_mcp/openapi` snapshot, exactly as the publisher does, and the regeneration residue was discarded. No repeated broad TS, Python, Go, PHP or Rust suites.

Machine-local receipts: MAIN `.claude/artifacts/ruby-management-2-final/` — `patch-before-rebase.diff` (all eight files byte-identical to `local-file-bindings.json`), `main-floor-red.log`, `guardrail-red-rebased.log`, `guardrail-green-rebased.log`, `check-ruby-rebased.log`, `canary-openapi-20260918.log`, `installed-*-rebased.log`, `rebased-checks-summary.txt`, `commit-ci-hook.log`. The runtime directory was removed after the run; no credentials, tenant resources or production writes.

## Release boundary

Status: source corrections on a PR to `main`, not merged or published. After merge, release-please regenerates PR234; ROOT applies the held hunks on that branch immediately before merging it, the publisher publishes the gem, and installed public-gem acceptance follows. No release-please config, version file or manifest changed here; retiring the fulfilled `release-as` and refreshing PR223's floors remain follow-ups.
