# v1.8.250 regeneration patch wave (2026-09-21)

Status: shipped. Eleven Release Please candidates carried the v1.8.250 schema regeneration (nullable references as canonical unions, `MailboxMessage` header fields) across TypeScript, Python, Ruby and Rust, plus the CLI 1.7.2 distribution tail. Each candidate was qualified by a lane (L20 TypeScript, L21 Python, L22 Ruby/Rust) in a detached worktree at its bot head, then merged by ROOT in ecosystem order with a changelog lead applied on the release branch immediately before merging; every publication was read back against a candidate-owned build at its tag. Related tracked evidence: `evidence/rp-empty-commit-fence-20260921.md` (the Release Please zero-file-commit fence that preceded the wave), `evidence/mcp-2-1-3-candidate-20260921.md`, `evidence/v1-8-250-regen-20260921.md` (the regeneration the wave publishes).

## Shared gates per candidate

`pnpm canary:openapi` ("Live OpenAPI specs match committed snapshots."), `pnpm drift:check`, `node scripts/publication-guard.mjs candidate --sha <head>` (app `b42ce87003ae8d4ac80024ce9796cd96db97c5439c5e99d957682f6b6a1f5999`, sending `c1f82f9b8944026571d9e66ae84d4bef90e6c575cc8a57127afcdc6e90aa7b0e` = `Sendmux/docs` main = live), `pnpm test:release-state`, `pnpm test:publication-guard`, the ecosystem build/test gates, a packed artifact hash, and an absence readback before merge. Lane reports: `.claude/artifacts/l20/report.md`, `l21/report.md`, `l22/report.md` (machine-local).

## Python

- python-mailbox 2.0.2 — #283, bot head ebc807ef, lead 8e4b896, merge 3df6ddcf, tag `python-mailbox-v2.0.2`, run 35573526331. PyPI wheel 248033 B `7f3035ce339446ad1ad2c0ea51215ef9095d7722d9be84fa69a2db5adfc3f54b` = qualified build; sdist 82487 B `1aa920bfe779ab34…` = the tag tree (only PKG-INFO/.gitignore are sdist-only; the pre-lead build differed by the CHANGELOG lead only). Fresh venv: 2.0.2 installs, `MailboxMessage` exposes `message_id`, `in_reply_to`, `references`, `reply_to`.
- python-management 2.0.1 — #259, bot head ff55a093 (refreshed after the mailbox merge; L21 qualified ba29e849 with the same content), lead + stale-Unreleased drop 121eb85, merge d7180616, tag `python-management-v2.0.1`, run 35576310752. PyPI wheel 253450 B `b54079b5a7c300060d9f3f70dd385e018cbc88cd3fcc46746ac75c2d702609cf` = qualified build; sdist 81638 B `5ae35c176e170bee…` = the tag tree (0 differing entries).
  Fresh venv: 2.0.1 installs (core 1.3.1); `MailboxAppPasswordResult.credential` is annotated `MailboxCredential`; `MailboxAppPasswordResultCredential` resolves to the same class with a `DeprecationWarning`.
- floors — PR #300 (`fix(python-sdk)`, 466f5e4, merge 9713147e): `sendmux-mailbox>=2.0.2,<3.0.0`, `sendmux-management>=2.0.1,<3.0.0`; the guard (`checkPythonSdkDependencyFloors`) enforces umbrella floors == released component versions. Its `verify` and `python-runtime (3.11)` legs first failed only because PyPI's index had not yet served management 2.0.1 (08:18–08:19Z); the rerun passed.
- python-sdk 3.0.1 — #248, refreshed head 0c931e1f (re-qualified: canary, drift, `build:python:dists` 173 tests, guard, release-state 29/29, publication-guard 84/84, guardrails), merge 6ba79ec5, tag `python-sdk-v3.0.1`, run 35582447802. PyPI wheel 4289 B `51b2bcb95177368f3440ffe4c23259ae4d2d655e66eef5fdf4e2b2d5d4c1377b` and sdist 5411 B `2d1c273b730fb18b35b0848f202664d5b8a52740fd758f342de602c01fcba2f6` both equal the qualified build (no lead commit); `requires_dist` carries the new floors; a fresh venv resolves mailbox 2.0.2, management 2.0.1, sending 1.6.0, core 1.3.1.

## TypeScript

- ts-mailbox 2.0.2 — #293, refreshed bot head 7f9aba4 (guard 0, release-state 0), lead ba0bcbe, merge 63c3124f, tag `ts-mailbox-v2.0.2`, run 35585259437. npm latest 2.0.2 (`dist.shasum` 17b9a143…); the published tarball's 60 members are byte-identical to a candidate-owned `pnpm pack` at the tag (sha256 `933a3c1867db0a0e3703dcc5124a2af9760306eba4b27341836deea420454317`, equal to L20's qualified pack); fresh ESM consumer imports 45 exports at 2.0.2. Post-merge main CI green.
- ts-management 2.0.2 — #294, refreshed bot head 4cc37ca (guard 0, release-state 0), lead 831dafe, merge 47d44b2b (merged on local verification; main CI 35587388658 green), tag `ts-management-v2.0.2`, run 35587388653. npm latest 2.0.2; the published tarball's 54 members are byte-identical to a candidate-owned pack at the tag (sha256 `594fbcb70fada68695f47a2796bfad456bc5aca3947064f0695b720b6df74757`, equal to L20's qualified pack); fresh ESM consumer imports 56 exports.
- Allocation finding after ts-management merged: the Release Please run (35587388653) built no TypeScript candidate (ts-sdk "No user facing commits found since ts-sdk-v2.0.1", ts-cli 0 commits, ts-ai-sdk none) and its node-workspace plugin reported "Found 0 in-scope releases", so #295/#290/#291 kept the bot's original allocation (the ts-sdk manifest line rides in the ts-ai-sdk PR; the cli line is absent). Reconciled by hand on each release branch, in the shape the bot's own convergence produced in the 2.0.1 wave: merge `main`, set that package's single manifest line, verify `test:release-state` 29/29 and the publication guard, commit through the pre-commit drift check, merge.
- ts-sdk 2.0.2 — #295, bot head 5bc78b2 reconciled on the release branch (merge-from-main f375fa2 + manifest line commit 248a0ba; release-state 29/29; guard 0), merge e5e95d0e, tag `ts-sdk-v2.0.2`, run 35590716965. npm latest 2.0.2 (`dist.shasum` 4226e6db…), published dependencies core 1.2.0 / mailbox 2.0.2 / management 2.0.2 / sending 1.6.0; 8 of 9 tarball members byte-identical to a candidate pack at the tag and `package.json` field-for-field identical (serialisation order only); fresh ESM consumer resolves mailbox 2.0.2 and management 2.0.2. Post-merge main CI: 28/29 green, the Windows readiness-sensitivity leg re-run (flake class).
- ts-ai-sdk 0.5.2 — #290, bot head 741cd5c reconciled (merge-from-main commit 716eff4 with the manifest resolved to main + ai-sdk 0.5.2; release-state 29/29; guard 0), merge 16c90972, tag `ts-ai-sdk-v0.5.2`, run 35592783161. npm latest 0.5.2 (`dist.shasum` d7e5c7b9…, provenance logIndex 2905642086); published dependencies mailbox 2.0.2 / sending 1.6.0; 6/6 tarball members byte-identical to a candidate pack at the tag (sha256 `fbc4878105261ecb…`); fresh consumer with `ai` and `zod` resolves mailbox 2.0.2. Post-merge main CI green.

## Ruby and Rust

- ruby-mailbox 2.0.2 — #288, refreshed bot head 0052a7d (manifest, CHANGELOG, version.rb; guard 0; release-state 0), lead 9c76765, merge b3a613c4. (Tag, RubyGems readback and consumer receipts follow.)
  Tag `ruby-mailbox/v2.0.2`, run 35595240826 (publish-rubygems success). RubyGems latest 2.0.2; the published gem's 147 data members are byte-identical to a candidate-owned `gem build` at the tag; isolated `GEM_HOME` install and `require 'sendmux/mailbox'` succeed.
- ruby-management 2.0.1 — #263, refreshed bot head bc690e4 (guard 0, release-state 0), lead + stale-Unreleased drop 0abc11e, merge c5f143d0, tag `ruby-management/v2.0.1`, run 35596108471 (publish-rubygems success). RubyGems latest 2.0.1; the published gem's 145 data members are byte-identical to a candidate-owned `gem build` at the tag; isolated `GEM_HOME` install and `require 'sendmux/management'` succeed.
- rust 0.5.1 — #261, refreshed bot head d086dc3 (manifest, CHANGELOG, Cargo.toml, both Cargo.lock; guard 0; release-state 0), provenance lead f5360a2, merge be295dea, tag `rust-v0.5.1`, run 35597021526 (publish-cratesio success). crates.io max_version 0.5.1; the published `.crate` is byte-identical (sha256 `deabfe26ff7dc8f9…`, 13 members) to a candidate-owned `cargo package` at the tag; a fresh consumer crate pinning `=0.5.1` builds.

## CLI

- ts-cli 1.7.2 — #291, bot head db33b8d reconciled on the release branch (merge-from-main + manifest line 7815a75; release-state 29/29; guard 0), merge 8fc202a7, tag `ts-cli-v1.7.2` → 8fc202a7, run 35598117491 (release-please success; publish-npm: publish, pack, verify and attach steps success; its final "Update Homebrew tap" step failed because `npm view @sendmux/cli@1.7.2` still returned E404 thirty seconds after the publish — the same registry index lag seen on PyPI earlier in the wave). npm latest 1.7.2 (`dist.shasum` f8e407b0…, `dist.integrity` sha512-/Iir1w…, provenance v1 `gitCommit` 8fc202a79d… == the tag target), published dependencies `@sendmux/sdk` 2.0.2 and `@oclif/core` 4.11.4, 377 members (a candidate-owned `pnpm pack` at the tag has the same 377 members, 376 byte-identical and `oclif.manifest.json` JSON-equal — 111 commands, serialisation only); the registry tarball's sha512 fc88abd7…807f6c equals the decoded `dist.integrity` and its sha256 is 80813322…fe19. Five release archives `sendmux-v1.7.2-8fc202a-{darwin-arm64,darwin-x64,linux-arm64,linux-x64,win32-x64}.tar.gz` on the GitHub release. Homebrew tap PR Sendmux/homebrew-tap#12 (`homebrew/sendmux-v1.7.2`, formula url cli-1.7.2.tgz + sha256 80813322…) was opened by running the repository's own `scripts/update-homebrew-tap.mjs` from a checkout of the tag target once the index served 1.7.2; the run itself was not re-run so the verified release assets were not re-attached. Distribution tail (operator-approved 2026-09-21): Snap — PR #301 (`snap/snapcraft.yaml` → version 1.7.2, source cli-1.7.2.tgz, `source-checksum` sha512/fc88abd7…) merged c0e0b402; snap workflow run 35604328171 built and uploaded both architectures (edge revisions amd64 195, arm64 194); both revisions were then released to `latest/stable` with `snapcraft release`, and `snapcraft status` plus the public store channel map read back stable 1.7.2 rev 195 (amd64) / rev 194 (arm64). Chocolatey — `chocolatey.yml` dispatched on main 8fc202a7 (version 1.7.2, push=true), run 35602397036 success; the community feed lists `sendmux.portable` 1.7.2 (automated checks pending) and `sendmux` 1.7.2 (waiting on its portable dependency) in moderation next to 1.7.1 (portable passes all three checks, awaiting a human moderator); 1.6.0 remains the approved latest — external. Homebrew — tap PR #12 merge is scheduled for after the 24 h npm `--before` window (2026-09-22 ~12:40 UTC) with readback of `Formula/sendmux.rb`.

## Post-merge main CI

Every merge was followed by a green `CI` run on the resulting `main` head: 3df6ddcf, d7180616, 9713147e, 6ba79ec5, 63c3124f, 47d44b2b (35587388658), e5e95d0e (28/29 first pass; the Windows readiness-sensitivity leg re-run green — flake class), 16c90972, b3a613c4, c5f143d0 (35596108468), be295dea (35597021543), 8fc202a7 (35598117421). The Snap merge c0e0b402 is watched the same way. No bot-head CI wait preceded a merge once the refreshed head had passed the local re-checks (lane qualification, publication guard, `test:release-state`, hook-green lead); publication and main CI were verified afterwards, as the operator directed on 2026-09-21.

## Test pruning and rules

Pareto kill-list review of this programme's test diffs (Sendmux/sendmux #530/#535/#540/#541, Sendmux/sendmux-sdk #251–#300, Sendmux/skills #22/#23): 98 rows reviewed, 76 kept, 4 removed as duplicates or change-detectors with their surviving coverage named (Sendmux/sendmux PR #544, Sendmux/sendmux-sdk PR #302), 18 off-list concerns proposed and parked; report `.claude/artifacts/l13-final/pareto-rules-report.md` (machine-local). Three rule candidates were routed to the operator-local `AGENTS.md` for approval rather than applied.

## Machine-local artifacts

`.claude/artifacts/l20/`, `l21/`, `l22/` (lane reports and receipts), `.claude/artifacts/wave-tools/` (stage-lead, push-and-ci, check-candidate, npm/gem/crate readback scripts and their logs), `.claude/artifacts/ts-cli-1-7-2/` (publish-npm job log, tarball comparison, snapcraft status before/edge/after, snap merge-and-promote log, Homebrew tap script log, Chocolatey run), `.claude/artifacts/l13-final/` (Pareto and rules report, kill-list PR report). The ledger is `../sendmux-sdk-combined-native-release/.claude/plan.md`.

## Completion

```
✅ Complete (published) — every package in the wave is on its public registry and read back; Chocolatey 1.7.2 sits in external community moderation and the Homebrew tap merge is scheduled for after the 24 h npm pin.
Correctness: each publication compared member-by-member (npm/gem/crate) or by wheel hash + sdist tree (PyPI) against a candidate-owned build at the tag; npm provenance gitCommit == tag target for the CLI; Snap stable/edge revisions verified through snapcraft status and the public store channel map.
Tests:       no test changes in the release candidates themselves; kill-list removals in #544 / #302 with surviving coverage named in their commit messages.
Journeys:    fresh consumers per ecosystem (venv, ESM consumer, isolated GEM_HOME, consumer crate); CLI tarball members verified; no browser journey applies.
Evidence:    evidence/patch-wave-20260921.md — this file.
Status:      published to PyPI, npm, RubyGems, crates.io, the Snap Store (stable) and the Chocolatey moderation queue; Homebrew tap PR #12 awaiting its window.
Torn down:   release-branch worktrees reaped after each merge (git worktree list clean for every lead/recon/snap worktree; test ! -d verified); no containers spawned.
Parked:      fence `go` with exclude-paths now that 3.0.0 shipped; go otel bump (3.0.1); docs versioning-support.mdx "1.x release line"; proxy /@v/v3.0.0.info negative cache (informational); Pareto propose rows in the L13 report.
```
