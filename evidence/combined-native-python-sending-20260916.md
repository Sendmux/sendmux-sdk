# Python Sending 1.6.0 local release evidence

Status: local preparation complete; publication, production, and manual acceptance remain open.

- Candidate `e18be41a0ba9a2cd59739dcdc29e9b8aa08e5412`, based after accepted MCP `2.1.0`; Sending manifest, `pyproject.toml`, and generated runtime align at `1.6.0` with `sendmux-core>=1.3.1,<2.0.0` and no Sending `release-as` override.
- Changed only `packages/python/sending/CHANGELOG.md`: moved the exact `delivery_group` Features bullet from `Unreleased` to dated `1.6.0`; empty `Unreleased` retained. README install/API references remain accurate.
- ROOT initial `OPENAPI_INPUT_DIR=/Users/rj/Desktop/GIT-REPOS/sendmux-docs-mcp-oauth pnpm drift:check` passed at the actual candidate with no generated diff.
- `pnpm build:python:dists` passed source native/SDK/LangChain cohorts, typing, packaging, and artifact validation: mypy reported no issues in 55 native, 1 SDK, and 4 LangChain source files; shared tests were 146/146 (136 native, 10 LangChain), zero skipped/xfailed/xpassed. Raw output: `.claude/artifacts/native-python-sending-1.6.0/build-python-dists.log`.
- Canonical `pythonArtifactConsumers` ran wheel and sdist native/SDK/LangChain cohorts with runtime wheel tests: 199 native and 10 LangChain tests passed, with 22 upstream A2A protobuf `DeprecationWarning` messages and no skipped tests. It isolated installs, passed `pip check`, verified archive bytes/provenance, and removed its exact workspace. Raw output: `python-artifact-consumers.log`.
- `pnpm prepare:publish:pypi` selected exactly `sendmux_sending-1.6.0-py3-none-any.whl` SHA256 `29b8a05fc6aa656105bd5912db5ef20ede43e04f17b96d484ad3bbb4cd63da33` and `sendmux_sending-1.6.0.tar.gz` SHA256 `4218af5036653f629e7ca0441c7a5c43678a5d9358f0e3bf4cf5c31725c64c9e`; copies are retained in `.claude/artifacts/native-python-sending-1.6.0/`. No publication occurred.
- `pnpm test:python-release-guardrails` passed. `pnpm test:release-state` passed 27/27 with zero skips.
- `OPENAPI_INPUT_DIR=/Users/rj/Desktop/GIT-REPOS/sendmux-docs-mcp-oauth pnpm canary:openapi` exited 1 as expected: live App and Sending schemas lack combined provider-variable/delivery-group and mailbox fields. This is the open downstream production gate, not a native-package failure.
- Owner wrappers `41568`, `44161`, `48536`, `49025`, `50081`, `50149`, `50187`, and `51049` were observed closed. The two early consumer attempts failed before execution due to scratch-runner quoting and the MAIN-artifact symlink import resolution; the corrected canonical runner closed every logged child and removed workspace `sendmux-python-sending-1.6.0-artifact-consumers-10zSdp`. Source tests reported no warnings or skips; consumer warnings are qualified above.
- ROOT owns all remote writes, publication, deployment, tags, registry confirmation, and manual acceptance.
