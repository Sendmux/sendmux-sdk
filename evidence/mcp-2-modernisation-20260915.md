# MCP-2 modernisation: SDK source and verification evidence

Prepared 2026-09-15 20:52 AEST. Runtime candidate: `f7c00797fb03143120515c32e3e8e9a4741fd3b1`; subsequent test-only pruning: `71f93d8948c5fcee75c526aa6be6c719e222cf62`; base: `126685869633ee469e64138cd5b0c5641e4ad8ae`. Status: coded and independently reviewed, not merged or deployed. Package publication, final hosted/runtime checks and production/manual acceptance remain open. Line references describe the named source slice; test-only pruning shifts later lines without changing their behavior.

## Status and scope

The records support the scoped SDK, generator, CLI, MCP, packaging, and verification-harness fixes below. They do not certify production deployment, public package availability, the final hosted matrix, or manual Atlassian acceptance. Original slice evidence ends at `e540627596ce478aaffca34180fa4414c80cdbc7`; later metadata/recovery fixes were reviewed through `06792b050b68bba19b1fe3dc4814c245333f1b2c`, the typing correction through `2a15a54caef8068e1f2bbfaad50cb0e11e3f390f`, mailbox-major preparation through `57c43c47f9adc1d5c16bdc6904b5681794b8ab1f`, and dependency-major validation through `f7c00797fb03143120515c32e3e8e9a4741fd3b1`. Evidence for one range is not silently promoted to another.

Generated clients and contract metadata remain generator-owned. No v1 adapter, metadata projection/drop, or permissive replacement schema was introduced. The final mailbox-major slice changes import/release identity and documentation, not runtime request count, payload limits, retries, or shared-state ownership. Earlier bounded transport/attachment changes are described separately below.

## Defects, root traces, and public regression seams

All unqualified paths below are relative to this SDK repository. App paths explicitly identify the separate producer repository.

| Scoped defect and repair | Source / behavior evidence |
| --- | --- |
| MCP attachment tools exposed server-local file paths; inline/delegated workflows replace that public input. Boolean JSON schemas needed semantics-preserving FastMCP translation, and successful raw tool output needed schema validation. | `packages/python/mcp/sendmux_mcp/server.py:72`, `:192`, `:241`, `:952`; public discovery/call tests `packages/python/tests/test_mcp.py:310`, `:562`, `:1119`, `:1146`, `:1238`. |
| Consumed gzip responses retained incompatible encoding headers; cancellation could interrupt owned response closure, while zero-delay retries could overtake pending cancellation. | `packages/python/mcp/sendmux_mcp/server.py:103`; `response_ownership.py:32` and `retry.py:22` in the same directory; public tests `packages/python/tests/test_mcp.py:350`, `:372`, `:410`. |
| Live evidence could accept wrong ownership, missing authority/request identity, incomplete persistence, timed-out children, or provenance-free audits. The safety contract distinguishes an attempted run/recovery ledger from a certified run. | `scripts/live-e2e-contract.mjs:73`, `:104`; behavioral fixtures `scripts/test-live-e2e-safety.mjs:72`, `:129`, `:279`, `:330`, `:443`, `:499`, `:1023`. |
| Contract checking could rewrite drift, load source-only provenance incorrectly from installed packages, or accept future native-version drift. Test-runner deadline/descendant ownership and nested test execution also needed real process-boundary checks; this is not a generator-native deadline claim. | `packages/python/mcp/sendmux_mcp/contract.py:164`; `packages/python/mcp/tests/test_contract.py:166`; `scripts/ci-consumers.test.mjs:9`, `:23`, `:62`. |
| Rust accepted foreign request targets, lacked cursor pagination on six list interfaces, and could not represent both attachment forms. | `rust/src/core.rs:84`; `rust/src/sending.rs:124`; external contract tests `rust/tests/public_contract.rs:102`, `:165`, `:406`. |
| Bundled Redocly YAML parsers were outside the root dependency override; empty merge mappings escaped merge-work accounting. The version-pinned vendor patch applies the upstream mapping/key charge and source-count cap. | Real installed-CLI regression `scripts/test-redocly-merge-budget.mjs:26`; four documents distinguish ordinary merges from populated, empty-source, and oversized-source attacks. No audit suppression replaces the patch. |
| Chocolatey publication could place a synthetic API key in child argv, and native PowerShell failures could be masked. | `.github/workflows/chocolatey.yml:239`; Windows publisher tests exercise encrypted configuration, keyless pushes, independent failure propagation, and exact configuration restoration/staging removal. |
| PHP generation reused a sibling version; aggregate Composer constraints did not enforce independent Mailbox-major adoption. | `scripts/generate-php.mjs:12`; `scripts/generate-php.test.mjs` tests `Mailbox generation can change version without changing sibling packages` and `PHP split verification honours independent versions and the aggregate Mailbox range`. |
| Agent registration readiness failed to recognize the nested API error; forbidden responses must not become retries. Attachment upload keys also needed stable retry identity without collapsing distinct outer sends. | `packages/ts/cli/src/agent-auth.ts:345`; `packages/ts/sending/src/node.ts:105`; CLI replay/namespace checks `scripts/check-cli.mjs:946`, `:958`, readiness checks `:1219`, `:1230`. |
| Frozen Python API exceptions broke normal context-manager traceback assignment, masking the actual API error. | `packages/python/core/sendmux_core/errors.py:9`; `packages/python/tests/test_core.py:147` (`test_api_error_survives_context_manager_cleanup`) and async counterpart `:170`. |
| Sending attachment helpers could upload an invalid combined count and read unbounded/changing files. They now reject over 10 combined attachments before file/upload work and bound file reads at 18 MiB plus overflow detection. | `packages/ts/sending/src/node.ts:15`, `:94`, `:144`, `:150`, `:155`; CLI uses the shared Sending loader; Python generation owns its corresponding guards. Exact-boundary, changed-after-stat, empty-file, and existing-reference cases are retained. These are helper bounds, not a claim that every server accepts that maximum. |
| Agent auth accepted redirects, unbounded or unfinished bodies, and operations exceeding the intended deadline; stream errors could escape the safe error boundary. | `packages/ts/cli/src/agent-auth.ts:23`, `:229`, `:360`, `:420`, `:430`; public CLI deadline cases `scripts/check-cli.mjs:1503`, `:1521`. Redirect fixtures establish local same-origin forwarding sensitivity, not a live cross-origin leak. |
| Strict generic response metadata rejected legitimate state-bearing list/wait envelopes. Repair belongs in producer metadata families and the custom wait schema, not response stripping. | **App repository:** `src/app/api/v1/_lib/schemas/mailbox-api.ts:249`; **SDK:** `packages/python/mcp/sendmux_mcp/server.py:596`, `:818`; tests `packages/python/tests/test_mcp.py:272`, `:705`, `:1309`. Generated consumers retain typed state and endpoint-specific required thread identity. |
| Failed workflow runner status could be replaced by writer status; failed-run recovery must survive without authorizing historical audit evidence. | `.github/workflows/live-e2e.yml:119`, `:168`; `scripts/live-e2e-contract.mjs:73`; behavioral parsed-shell tests `scripts/test-live-e2e-safety.mjs:837`, `:893`. Exact hidden recovery files are uploaded, not arbitrary run directories. |
| The metadata fixture's heterogeneous dictionary unpack failed mypy despite valid runtime semantics. | `packages/python/tests/test_mcp.py:281`: name the homogeneous `wait_meta` mapping; strict positive/negative assertions at `:287` remain intact. Three test-fixture lines changed, no typing escape or production relaxation. |
| Corrected thread-list response identities are public compatibility breaks, including Go interface membership, Python/Ruby classes, and constructed TypeScript/PHP models. | `go/mailbox/oas_response_decoders_gen.go:4593`; candidate module `go/go.mod:1`; external `go/mailbox/response_contract_test.go:13` (`TestListResponseMetadataContracts`). Ordinary lists retain their distinct result type. |

## Observed REDs and retained receipts

These are historical run/mutation receipts, not newly rerun tests. A mutant RED demonstrates sensitivity to the named realistic regression; it does not establish all neighboring assertions. Paths in the receipt column are relative to `.claude/artifacts/` unless explicitly stated otherwise.

| Behavior | Observed failure / later evidence | Receipt |
| --- | --- | --- |
| MCP cancellation and ownership | `close_started=True, close_finished=False`; timeout `DID NOT RAISE TimeoutError`; zero-delay cancellation made two attempts instead of one. Focused post-fix retry/adapter/hosted gate passed 19 tests. | `task-3-ea207b3-parent-cancellation.log`; `task-3-finding19-focused.log` |
| Live authority, identity, audit and child completion | `Missing expected rejection`; timeout ordering was `[abort,cleanup]` rather than `[abort,finished,cleanup]`; fatal recovery retention failed `false !== true`. | `task-4a-final-20260912-1140/{request-identity-red-all,request-identity-green,child-timeout-red,child-timeout-green}.log`; `task-4a-review2-20260912-1232/cli-home-{red,green}.log` |
| Read-only contract drift / packaging / process ownership | Drift was accepted and five files rewritten; deliberate packaging early-return mutant failed all eight guards; timeout/descendant mutants failed public process controls. | `task-4b-20260912/{contract-drift-red,contract-drift-green,artifact-mutant-red,process-mutant-red,process-boundaries-green}.log` |
| Rust origins, pagination, attachment variants | Foreign-target case performed 10 HTTP requests, expected zero; missing pagination produced `E0609` / `E0599` at the public consumer interface; attachment cases rejected the supported reference form. | `task-4c/{rust-origin-red,rust-pagination-red,rust-attachment-red,rust-three-slices-green}.log` |
| AI peer acceptance and actual nested execution | Unsupported Zod 3.24 was accepted; an intentionally failing nested child was not rejected because inherited `NODE_TEST_CONTEXT` skipped execution. Corrected final runtime receipt includes the previously omitted OAuth cases. | `task-4c/{ai-peer-red,nested-test-red,runtime-contracts-final-verified}.log` |
| Bundled YAML budget | Both unpatched Redocly 2.51.2 and 2.53.0 accepted empty/oversized merge-source attacks: `accepted: true`, `expected_error: false`. Installed patched CLI rejected both; ordinary merges remained valid. | `task6-chocolatey-green.wGRDTQ/merge-budget-tracked-red-{2.51.2,2.53.0}.log`; `merge-budget-green-installed.log`; `merge-budget-generation-parity.log` in that directory |
| Publisher credentials and native exit propagation | Windows RED run `34915059714` observed `apikey` where the inert publisher expected `push`; no credential value is reproduced here. Native-exit RED `34729944696` showed exit 7 masked by a subsequent success. | `task6-chocolatey-red.Gsunop/`; `task-4c/windows-native-exit-red-34729944696/`; `task-4c/windows-native-exit-green-34730367678.7SdCsS/` |
| PHP independent release identity | Generated user agent stayed at the sibling version; real Composer accepted the deliberately incompatible aggregate constraint. Post-fix generation changes only the intended surface and Composer rejects the old range. | `task6-php-versions.lF3aCi/{generator-red,generator-green,splits-red,splits-green,future-version-red,future-version-green}.log` |
| Readiness and upload replay | Nested readiness returned HTTP 503 after registration; forbidden-response mutant retried. Identical attachment retry returned 409; distinct-namespace, content-key, ordinal, unkeyed and explicit-key controls detect their named regressions. | `task6-cli-readiness.SM1YtG/{nested-red,nested-green,forbidden-mutant-red}.log`; `task6-attachment-replay.blaGhK/` |
| Python API exception propagation | `FrozenInstanceError: cannot assign to field '__traceback__'` replaced the real API failure; sync and async context-manager tests are green after the exception fix. | `task6-python-exception.9Vk7P8/{sync-red,both-red,full-green}.log` |
| Attachment preflight and byte limits | Invalid count caused uploads: TS `12 != 0`, CLI expected 20/actual 32, Python 11 uploads rather than none. Size/growth/truncation/empty cases and exact-limit mutants have separate retained REDs. | `task6-attachment-count.F7gyAU/`; `task6-attachment-bytes.yHOWdI/` |
| Bounded agent auth | Size check expected one rejection, got zero; unfinished stream stalled; truncated body leaked `terminated`; registration/token-body deadline REDs exceeded the 20-second fixture guard. Corrected deadline closes at approximately 15 seconds. | `task6-agent-http.zTGlSx/`; `task6-agent-deadlines.ok9doz/` |
| Real mailbox metadata and recovery exit precedence | Six legitimate producer envelopes went from 0/6 to 6/6 validation; MCP reported `Tool returned data that does not match its declared output schema.` on the old schema. Old workflow returned writer status: `27 !== 19`. | `task6-metadata-contract.k2Yfy2/{app-before-validation,sdk-after-validation}.log`; `task6-final-review-late-gates/i2-e540-behavioural-red.log` |
| Strict metadata sensitivity | Later detached mutations separately cover required optional state, invalid state type, optional request identity across the four metadata families, optional thread identity, permissive MCP metadata, and optional MCP request identity. App untouched result is 19/19. | `task6-final-review-late-gates/app-*-mutant.log`, `mcp-*-mutant.log`, `app-metadata-green.log`, `mcp-metadata-green.log`, batch `.exit` files and `SHA256SUMS` |
| Mypy fixture | Two nested-dictionary inference errors at old line 287; corrected mypy exits 0 and focused MCP suite passes 44/44 without changing validators. | `task6-final-review-late-gates/{mypy-fixture-red,mypy-fixture-green,mcp-full-after-mypy}.log` |
| Go response metadata | Real HTTP decoder mutants clear ordinary `SyncState` or thread `ThreadID`; assertions observe unset state / `ThreadID:""`. Unchanged public test then passes. | `task6-mailbox-major-release/{go-ordinary-sync-field-loss-red,go-thread-identity-field-loss-red,go-mailbox-contract-final-green}.log` |
| Published Python floor | Existing guard rejects `>=1.5.0,<2.0.0`, requiring already-published `>=1.5.1,<2.0.0`; one-line floor repair passes the unchanged guard and release guardrails. | `task6-mailbox-major-release/{python-sdk-floor-red,python-sdk-floor-green,python-release-guardrails-green}.log`; `pypi-mailbox-1.5.1.json` |

Setup/build failures are not product-behavior REDs: removed API/missing export/import bootstrap errors; malformed fixtures or wrong operation flags; Go's initially missing `/v2` import, invalid `next_cursor:null` fixture and unchecked response-write lint; Python replay fixture missing `html_body`; Windows CRLF extraction/no-results and wrong-lock-handle attempts; the Rust sensitivity run missing `README.crates`; the Xcode licence gate. Rust dependency edition/toolchain failure and Go module-import failure establish consumer/build incompatibility only. The first Python empty-file probe was already green and is not credited as a new regression demonstration.

The own-diff authored-test audit is recorded in `.claude/task-6-sdk-test-pruning-ledger.md`. It maps additions to the behavior/RED receipt families above and records original replacements without inferring every assertion's RED from aggregate success. Five obsolete MCP client-root/file-path cases were replaced by local/hosted schema omission and safe-mode checks as an intentional breaking API removal; their old error semantics are not retained. Nine obsolete live-runner source-shape assertions were replaced by real abort/deadline/late-completion/recovery tests in `scripts/test-live-e2e-safety.mjs`. The old inline wheel checker is superseded by `scripts/check-mcp-artifacts.py` plus real omission/corruption tests, preserving all prior required members.

Pre-PR pruning `f7c00797..71f93d8`: Tests +0; −`scripts/test-chocolatey-workflow-order.mjs` environment/transaction-shape block — source-text duplicate, still covered by real `chocolatey-push` healthy/early/final cases and `assertPublisherTransaction` in `scripts/test-windows-workflow-native-exits.mjs`; −`packages/python/tests/test_mcp.py:test_mailbox_upload_attachment_omits_file_path_from_public_schema` — exact duplicate, still covered by `test_mailbox_upload_attachment_public_schema_has_only_safe_input_modes`. The key-option source guards and normal Windows CI wiring guard retain unique coverage and remain. Other source-shape/internal-version assertions with no proven replacement remain explicitly qualified, not blanket-deleted. Post-prune Node gate passed; MCP file 43/43 passed with zero skips, owner8822 verified absent. Independent deletion review: spec compliant, quality Approved, no findings. Runtime/workflows/generated artifacts are unchanged from the full-build source.

## Independent review acceptance and limits

The whole-branch review of `1266858..e540627` inspected the 55-commit candidate and returned Ready With Fixes: I1 strict mailbox metadata and I2 failed-run recovery. Both were corrected in the separately reviewed `e540627..06792b0` batch. That review covered authored workflow, CI-consumer, native and generator changes; it did not independently inspect every generated/lock byte or every test body. Later scoped reviews extend the reviewed source through `f7c00797`, not production approval. The following are the scoped verdicts. The task-review method did not rerun suites; it inspected code/diffs and supplied receipts independently of the implementer.

- Original MCP Task 3 review chain resolves findings 13–19 through `76a7604`; original safety Task 4a ends at `e82d641`, contract Task 4b at `3703a89`, and runtime Task 4c at `46c36fe`.
- Task 6 accepted ledger covers YAML `3372b899..9e3260e`, Chocolatey `8ed6246..3372b899`, PHP generator `9e3260e..2a875d6`, readiness `2a875d6..691ed25`, replay `691ed25..5549549` plus namespace correction `b78bfeb..682dc86`, Python exception/bounds `5549549..83cb780`, and agent auth `83cb780..b3f0129`.
- PHP adoption/security review includes `b3f0129..8e7da51` and `523e9a4..02e28d2`; release-intent review includes `8e7da51..1705bc0` and `02e28d2..e540627`; Windows OAuth `1705bc0..b78bfeb` was approved after ownership fixes.
- Metadata/recovery `e540627..06792b0`: source spec compliant, quality Approved, no Critical/Important source defect. Its first sensitivity addendum was narrower than “every strict boundary” and lacked raw references; later separate receipts now exist in `task6-final-review-late-gates/`. ROOT authenticates those late receipts; the earlier review is not retroactively described as having inspected them.
- Mypy `06792b0..2a15a54`: spec compliant, quality Approved, no findings; exact three-line fixture repair preserves strict validation.
- Mailbox-major `2a15a54..57c43c4`: spec compliant, quality Approved for source/release intent, no Critical/Important findings. Qualifications: positive Go decoder coverage is not an exhaustive rejection suite; package compilation is not compilation of every README snippet; real release changelogs must include historically nonconventional commit subjects ignored by the preview parser.

- Python dependency-major guard `57c43c4..f7c00797`: spec compliant, quality Approved, no Critical/Important findings. Shared SDK/MCP validation derives each dependency's ceiling from its own declared major; changed consumers must match the release manifest, unchanged consumers may retain a coherent older range. Existing 1.x assertions remain unchanged. The public mixed-major fixture failed before the fix (`Got unwanted exception`, old `<2.0.0` bound) and passes after it; release-state 27/27 and Python mypy 58 / pytest 143 pass. Three range-mutant logs share an unlabeled assertion, so separate attribution is implementer-reported; the later diagnostic assertion has no separate observed RED. Raw receipts: `task6-python-major-floor/`, 24 hashes independently verified by ROOT.

Local review sources: `.claude/task-6-original-source-evidence-index.md`, `.claude/task-6-independent-reviews-20260915.md`, `.claude/task-6-sdk-final-fix-review-06792b0.md`, `.claude/task-6-mypy-fixture-review-2a15a54.md`, `.claude/task-6-mailbox-major-release-report.md`, and `.claude/task-6-mailbox-major-release-review.md`.

## Local, hosted, and conformance evidence

The latest task-bound `OPENAPI_INPUT_DIR=... pnpm build` completed with exit 0 at `f7c00797fb03143120515c32e3e8e9a4741fd3b1`: drift/generators, TypeScript package/public/layout/tree-shaking/static/release checks, Go tests/vet/lint, Python, PHP, Ruby, MCP, required conformance, and connection adapters 4/4. Receipts: `.claude/artifacts/task6-python-major-floor/full-build.log` and `.exit`; ROOT independently verified all 29 receipt hashes. The previous full aggregate at `57c43c47` also passed, with Python mypy 58 / pytest 143 and MCP mypy 30 / pytest 154; its separate 51-file manifest remains verified. Exact source ranges are preserved rather than attributing older receipts to a later commit.

Conformance is deliberately not summarized as all tests passing. Original frozen required accounting was legacy 70 SUCCESS, modern 114 SUCCESS + 1 INFO + 5 capability skips. Eight optional modern Tasks-extension scenarios had nine failing checks: non-scored, still failures, not passes. The latest aggregate visibly reports `181 passed, 9 failed`; the report classifies all nine as explicitly non-scored extension failures for the pinned protocol. Required-scenario selection/validation lives at `scripts/mcp-conformance.mjs:52`, `:59`. Retained raw totals and required/optional classification must travel together.

Historical Windows acceptance is real hosted evidence for those commits, not the final matrix. Latest reviewed run `34944800069` at `b78bfeb`: OAuth 13 passed / 0 skipped; 21 CLI processes closed; 11 temporary directories plus default configuration removed; expected user/SYSTEM/Administrators ACL; 15 native cases with 17 shell handles absent; publisher statuses 0/7/7 and push counts 2/1/2; three configurations restored and staging absent; installed Chocolatey 2.7.4 exercised inert authentication. Receipt root: `.claude/artifacts/task6-review-readiness.oXgaHA/windows-b78bfeb-34944800069/`. Forty synthetic command PIDs were recorded but individual absence was not asserted.

Existing warnings remain visible: generator header casing/untyped items; earlier YAML gate's 22 A2A deprecations, one MockTimers warning and two Oclif notices; cross-repository docs installation's five high advisory findings. Historical SDK audit counts are not current universal clean-audit evidence. App unit/integration receipts are separate producer evidence, not SDK publication or signed-in production journeys.

## Resource ownership and retained gaps

- Final aggregate `f7c00797`: ROOT independently verified 107 distinct recorded PIDs plus two owner process groups absent and port `63527` refused. Required conformance outputs were preserved intact under `task6-python-major-floor/conformance-2026-09-15T10-54-48.326Z`; the original task-owned `.tmp` path is absent. No product source changed during that build.
- ROOT verified all 51 mailbox-major receipt hashes and 178 unique logged PIDs absent; separately verified recorded build owners/groups, ports `58458` and `59846` refused, and exact retained CLI/repro paths absent.
- Latest aggregate owner PID/PGID `38568` is recorded absent by exact signal-zero checks in `task6-mailbox-major-release/full-build-57c43c4-cleanup.verify`; its CLI server port `59846` closed and owned workspace was removed. Failed preceding aggregate owner `23393` is likewise absent in `full-build-dc01472-cleanup.verify`.
- Earlier corrected mypy aggregate owner PID/PGID `76614` is recorded absent in `task6-final-review-late-gates/full-build-2a15a54-cleanup.verify`. Per-slice cleanup receipts remain in the artifact roots above; later ownership success does not close an earlier unknown handle.
- Historical Windows run `34687668655` retained `C:\Users\RUNNER~1\AppData\Local\Temp\sendmux-windows-ownership-hrKZNm` and `D:\a\sendmux-sdk\sendmux-sdk\.tmp\windows-job-sensitivity-x99Lvz` after PowerShell removal hit `EBUSY`. Locker identity and teardown were not proved. Source record: `.superpowers/sdd/task-4c-report.md:131`; raw `.claude/artifacts/task-4c/windows-membership-34687668655/workflow.log:981`, `:1004`. Do not label these paths removed because later fresh Windows runs passed.
- Original namespace-fix raw streams were not persisted. ROOT recovered tool evidence recording 143 PIDs, seven paths and two ports absent, but missing generator/default-pytest-root handles remain a qualification, not a reconstructed receipt.
- Artifact paths are retained machine-local evidence, not automatically available in a source PR. The worktree's `.claude/artifacts` is a symlink to MAIN's artifact directory: reaping this worktree does not delete those retained artifacts. The tracked summary carries the evidence needed for review; sanitized raw receipts remain available locally. No credential values, provider payloads, or customer content are included here.

## Remaining release and acceptance gates

Go's candidate identity is `sendmux.ai/go/v2`, with repository subdirectory `go/` and intended tag `go/v2.0.0`; official rules require the major suffix and subdirectory tag prefix ([Go major versions](https://go.dev/doc/modules/major-version), [module reference](https://go.dev/ref/mod)). The incompatible exported models require major treatment ([SemVer rule 8](https://semver.org/spec/v2.0.0.html#spec-item-8)). Package documentation explicitly marks the candidate unpublished; public root README facts remain published-version facts.

- Actual release-PR/native-version/changelog/manifest reconciliation remains open. Pinned Release Please 17.6.0 local preview selects intended v2 Mailbox/umbrella/Go/MCP and 0.5 Rust/AI versions; it does not certify remote plugin/tag behavior or refreshed release PRs. Inspect actual changelogs and remove one-time overrides only at the agreed release-owner point.
- As each intended native release actually exists, refresh the Go/package README candidate/unpublished qualifications and the root published Packages table from exact registry/version/tag readback. Source candidates and local preview versions must not be described as published; release-as cleanup alone does not make documentation current.
- Dependency-first adoption and future native floors remain open across affected packages. Current Python SDK floor is only the already-published correction `packages/python/sdk/pyproject.toml:22` (`>=1.5.1,<2.0.0`), not v2 adoption. No native version was pre-advanced by mailbox-major preparation.
- Tags, public registry publication, exact-version installed consumers, signed distributions, and final hosted/runtime matrices require fresh release evidence. Local replaces, previews, and packed fixtures do not prove public installation.
- Cross-repository documentation/skills, app deployment, signed-in production journeys, real mailbox/Sending live acceptance, and manual Atlassian acceptance remain separate open gates under ROOT's existing authority and resource constraints.
- Missing evidence must stay explicit: original unpersisted namespace streams/handles; historical Windows cleanup; individually unattributed original test REDs; any final hosted/publication/manual result not yet supplied. No absent test run, package publication, or live verification is implied by this evidence.

## Coordinated input publication

The snapshot-only docs commit `e4535b4518d8a321a451c4c1691dfb32b4462c7a` was fast-forward-published to `Sendmux/docs` main before SDK CI, whose normal snapshot checkout follows that default branch. Remote readback matched; [Verify docs run 34960218763](https://github.com/Sendmux/docs/actions/runs/34960218763) succeeded. All six affected generated operation pages passed local rendered metadata/requiredness checks with no console/dependent-request errors. ROOT viewed all eight screenshots and independently verified exact preview processes/groups, profile/cache paths and port absent. The separate authored docs and draft changelog were not published by that snapshot push.

## Hosted CI repair batch — 2026-09-15 22:10 AEST

Source commits `1ef2d70fef67d7e62c77b616cbb4028453ceb091` and `0abe86b7434dec939ba513f1700fda6eebe624aa` repair the observed PR 215 CI failures and a process-cleanup failure exposed by the clean verification run. No public package version, dependency floor, action pin, generated API, TLS policy, or live-E2E authority changed. All nine changed files were inspected by ROOT; independent scoped review of `5ce3cf2..0abe86b` is spec compliant and quality Approved, with no findings. That review preceded the aggregate's terminal result and is not represented as having observed it.

| Observed failure / escape | Repair and regression proof |
| --- | --- |
| Python matrix shallow checkout could not determine changed files; warmed local history concealed it. | `.github/workflows/ci.yml:151` fetches full history. Exact shallow-base RED: `Could not determine changed files for Python release guardrails. Configure actions/checkout with fetch-depth: 0...`; unchanged guard passes with full history. |
| PHP independent split installs resolved unpublished siblings from the registry; the aggregate-only checker missed this workflow-only step. | `scripts/check-php-splits.mjs:22` supplies only declared sibling path repositories to temporary candidate roots and independently installs/platform-checks all five roots. New real-Composer test RED: `core split was not installed independently`; synthetic versions then resolve through all five candidate autoloaders. Publishable manifests remain unchanged. |
| PHP 8.5 static analysis rejected the nullable initial cursor key. | `packages/php/tests/CoreTest.php:53` maps initial cursor to `<initial>`. Retained hosted RED: `Invalid array key type null` / `Possibly invalid array key type string\|null`. Local PHP 8.4.10 passes PHPStan, PHPUnit 47/47 with 234 assertions, and OAuth 10/10; hosted 8.5 confirmation remains pending. |
| Chocolatey parser expected LF in a Windows CRLF checkout. | `scripts/test-chocolatey-workflow-order.mjs:6` normalizes at the read boundary. New real-checker LF/CRLF test observed RED `Missing Chocolatey workflow step: Attach CLI ZIP assets to release`, then GREEN 1/1. Existing safety assertions remain. |
| Clean aggregate invoked live-E2E coverage before installing/building its language consumers. | `package.json:7` moves the unchanged check after language/MCP builds. Clean-base Python-adapter RED failed `false !== true`; final clean aggregate passes coverage for 104 OpenAPI and two custom MCP operations plus all four connection adapters. |
| First clean build succeeded, but its subsequent runtime suite failed at post-SIGKILL group probe `scripts/ci-consumers.mjs:86` with `kill EPERM`. | Negative-group signal-zero EPERM now means absence unproven, with the existing bounded poll requiring ESRCH. Apple's `killpg1` skips zombie members and can return EPERM for an existing group with no eligible members ([XNU source](https://raw.githubusercontent.com/apple-oss-distributions/xnu/main/bsd/kern/kern_sig.c)). The stack establishes a probe failure, not a denied SIGKILL; the exact historical process state was not captured at that instant. Transient-probe RED leaked `injected transient zombie-only process group`; persistent false-absence mutant RED raised `Missing expected rejection`. Correct behavior passes both and retains workspaces whenever shutdown is unconfirmed. |

Tests: +`scripts/generate-php.test.mjs:PHP split verification installs every candidate against synthetic sibling versions`; +`scripts/test-chocolatey-workflow-order-newlines.test.mjs:Chocolatey workflow-order checker accepts LF and CRLF workflows`; +`scripts/ci-consumers.test.mjs:a persistent group-probe denial cannot prove owned shutdown`; strengthened existing owned-descendant test with the transient probe boundary. Each has the observed RED above and GREEN. −none. Real signal-denial assertions and the two-second shutdown bound remain unchanged.

Fresh aggregate at exact `0abe86b` used a tracked clone with fresh frozen-lockfile installation, then full `pnpm build` and separate `pnpm test:runtime-contracts`: terminal exit 0, runtime contracts 7/7 with zero skips. Python pytest 142 and MCP pytest 153 passed; MCP emitted 22 existing warnings. Required conformance passed, while raw totals remain 181 passed / nine explicitly non-scored extension failures. Raw conformance (84 files) was copied before teardown. Both OpenAPI inputs were byte-identical to published docs `e4535b4`; authored docs source was not used as a substitute contract.

Receipts: `.claude/artifacts/task6-ci-215-fix/`, including `clean-aggregate-2.log`, `.exit`, `clean-aggregate-2-mcp-conformance/`, `clean-aggregate-2-resource-audit.json`, focused RED/GREEN logs and `SHA256SUMS`. ROOT independently validated all 157 hashes and checked 153 exact PIDs, two groups, 29 audited temporary paths plus the aggregate root/checkout absent; port 58046 refused. The first aggregate's raw conformance directory was deleted before copying; its log remains, and the later raw receipt does not retroactively repair that evidence loss. Failed fixture/setup attempts and their overwritten-log qualifications remain explicit in the private repair report.

PR 215's initial CI had 10/29 failed jobs; Chocolatey also failed. The complete repair batch requires a fresh hosted run after push. CodeQL alert 8 was independently traced to a JSON-escaped Node `-e` string literal, not HTML or shell interpolation, and answered in its own thread with no suppression. CodeRabbit explicitly skipped the large initial diff; its successful check is not an approval. Native publication, final hosted/AI matrices, package consumers, app/docs/skills pins, rollout and production/manual acceptance are still open.
